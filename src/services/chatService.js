import { View, Text } from 'react-native'
import React from 'react'

const chatService = () => {
    return (
        <View>
            <Text>chatService</Text>
        </View>
    )
}

export default chatService

// # Handles Firestore chat operations (send, receive messages)
